{
  "name": "readme-test",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "npm install",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "ts-node -e 'require(\"./src/index\").run()'"
  },
  "dependencies": {
    "@cubist-labs/cubesigner-sdk": "../../"
  },
  "devDependencies": {
    "@types/node": "^20.5.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.8"
  }
}
