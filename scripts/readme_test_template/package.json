{
  "name": "readme-test",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "npm ci --ignore-scripts",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node -e 'require(\"./dist/index.js\").run()'"
  },
  "dependencies": {
    "@cubist-labs/cubesigner-sdk": "../../packages/sdk",
    "@cubist-labs/cubesigner-sdk-fs-storage": "../../packages/fs-storage",
    "@cubist-labs/cubesigner-sdk-ethers-v6": "../../packages/ethers-v6",
    "dotenv": "^16.3.1",
    "otplib": "^12.0.1"
  },
  "devDependencies": {
    "@types/node": "^22",
    "ts-node": "^10.9.1",
    "typescript": "^5.5"
  }
}